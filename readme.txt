Zenbakia is a text incription App made with Android Studio and written in Java.
In the app, you can encrypt a string and send it to a friend via an external messager. Your friend can copy the encrypted message into their app and then decrypt it.

!!! RSA is not yet implemented -> encryption isn´t secure, if invader got access to java files !!!

APK can be found in /Zenbakia


